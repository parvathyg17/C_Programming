Ashwin Brototype
11:28
Design a BankAccount class. Keep balance private. Create methods for deposit() and withdraw(). Why is direct balance modification not allowed?

Create a Student class with private fields name, marks. Add setters/getters. Why is it risky to make marks public?

An Employee has a private salary. Show how getters/setters can ensure salary cannot be set to negative.

In a school system, make a base class Person (name, age). Create Teacher and Student as subclasses. What properties will you keep in Person and what extra goes to Teacher and Student?
